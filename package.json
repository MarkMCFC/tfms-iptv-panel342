{
  "name": "tfms-iptv-panel342",
  "version": "1.0.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create tfms_iptv_panel_db",
    "kv:create": "wrangler kv namespace create KV_CONNECTIONS",
    "db:migrate:local": "wrangler d1 migrations apply tfms_iptv_panel_db --local",
    "db:migrate:remote": "wrangler d1 migrations apply tfms_iptv_panel_db --remote"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
